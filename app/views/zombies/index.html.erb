<p style="color: green"><%= notice %></p>

<h1>Zombies</h1>

<div id="zombies">
  <% @zombies.each do |zombie| %>
    <%= render zombie %>
   <!-- <tr>
      <td><%= render zombie %></td>
      <td><strong>Rotting: </strong><%= zombie.rotting? %> </td>
      <br>

      <br>
      <td><Strong>Rank: </strong><%= zombie.roles.find_by(title: @title).title %></td>
      <br>

      <br>
      <td><strong>Brain Status:</strong><%= zombie.brain.status %></td>
      <br>

      <br>
      <td><strong>Brain Flavor:</strong><%= zombie.brain.flavor %></td>
    </tr>

    <p>
      <%= link_to "Show this zombie", zombie %>
      <%= link_to "Edit", edit_zombie_path(zombie) %>
      <%= link_to "delete", zombie, method: :delete, remote: :true %>
    </p> -->
  <% end %>
</div>
<%= form_for(Zombie.new, remote: true) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%#= link_to "New zombie", new_zombie_path %>
